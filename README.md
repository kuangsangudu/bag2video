# bag2video
a method to convert realsense bag file to [image|video] data

**e.g**

output video
```
  python test.py -i input.bag -o output.mp4
```
output images
```
  python test.py -i input.bag -o output\
```
